
# semantic graph vs syntactic tree.

the current Schema struct is the root of something that is more like a syntax tree. We need something more semantic where for example a Properties Type is not just a string but can be used to get the the node in the tree that represents that type.