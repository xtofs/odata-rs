[package]
name = "odata-rs-routing"
version.workspace = true
edition = "2024"

[lib]
name = "odata_routing"

[dependencies]
axum = "0.8.9"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
tower = { version = "0.5", features = ["util"] }
